{
  "name": "sinergi-main",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-worker.mjs",
    "preview": "vite preview",
    "lint": "node scripts/lint.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
